-- config.lua
Config = {}

-- Authentication details
Config.Hostname = "http://localhost:40120"
Config.Username = "admin" -- Fill with the username you set in txAdmin
Config.Password = "admin" -- Fill with the password you set in txAdmin

-- Settings
Config.Debug = false -- Set to true to enable debug messages